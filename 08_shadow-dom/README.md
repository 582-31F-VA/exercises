# Exercise: Shadow DOM

Implement a `<live-clock>` custom element that displays the current time
and updates every second. Its internals must be encapsulated in a shadow
DOM, and it must support a `format` attribute that lets users choose
between 12-hour and 24-hour formats.
